{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "minimumReleaseAge": "1 day",
  "lockFileMaintenance": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "Lock file maintenance cannot be age-validated (Renovate has no release timestamp to check against), so opt it out of minimumReleaseAge to avoid a permanently pending renovate/stability-days status. The package manager enforces its own minimum release age during re-resolution.",
      "matchUpdateTypes": ["lockFileMaintenance"],
      "minimumReleaseAge": null
    }
  ]
}
